<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.firmanjabar.favapp">

	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="com.firmanjabar.submission4.READ_DATABASE" />
	<uses-permission android:name="com.firmanjabar.submission4.WRITE_DATABASE" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".feature.favorite.FavoriteActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity
			android:name=".feature.tv_detail.TvDetailActivity"
			android:label="@string/title_activity_detail_tv" />
		<activity
			android:name=".feature.movie_detail.MovieDetailActivity"
			android:label="@string/title_activity_detail" />
	</application>

</manifest>